# ===============================
# EXAMPLE / TEMPLATE (DO NOT PUT REAL SECRETS)
# This project uses ENV vars (via .env / Jenkins Credentials)
# ===============================

# Active profile example (set in docker-compose / Jenkins)
# SPRING_PROFILES_ACTIVE=dev

# ---------- PostgreSQL (user DB) ----------
USER_DB_URL=jdbc:postgresql://<HOST>:5432/user_info?serverTimeZone=UTC&CharacterEncoding=UTF-8
USER_DB_USERNAME=postgres
USER_DB_PASSWORD=CHANGE_ME

# ---------- PostgreSQL (like DB) ----------
LIKE_DB_URL=jdbc:postgresql://<HOST>:5432/like_service?serverTimeZone=UTC&CharacterEncoding=UTF-8
LIKE_DB_USERNAME=postgres
LIKE_DB_PASSWORD=CHANGE_ME

# Optional Hikari tuning (like DB)
LIKE_DB_MAX_POOL_SIZE=10
LIKE_DB_CONNECTION_TIMEOUT=5000
LIKE_DB_CONNECTION_INIT_SQL=SELECT 1
LIKE_DB_VALIDATION_TIMEOUT=2000
LIKE_DB_MINIMUM_IDLE=10
LIKE_DB_IDLE_TIMEOUT=600000
LIKE_DB_MAX_LIFETIME=1800000

# ---------- MongoDB (upload) ----------
UPLOAD_MONGO_URI=mongodb://<HOST>:27017
UPLOAD_MONGO_DB=Upload

# ---------- MongoDB (notification) ----------
NOTI_MONGO_URI=mongodb://<HOST>:27017
NOTI_MONGO_DB=Noti